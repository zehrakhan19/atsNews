import React from 'react';

import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface HooksProps {}

export function Hooks(props: HooksProps) {
  return (
    <View>
      <Text>Welcome to hooks!</Text>
    </View>
  );
}

export default Hooks;
