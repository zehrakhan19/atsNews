import type { Meta } from '@storybook/react-native';
import { MaterialText } from './text';

const Story: Meta<typeof MaterialText> = {
  component: MaterialText,
  title: 'MaterialText'
};
export default Story;

export const Primary = {
  args: {}
};
