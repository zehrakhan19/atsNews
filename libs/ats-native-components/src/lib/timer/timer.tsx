import React from 'react';

import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface TimerProps {}

export function Timer(props: TimerProps) {
  return (
    <View>
      <Text>Welcome to timer!</Text>
    </View>
  );
}

export default Timer;
